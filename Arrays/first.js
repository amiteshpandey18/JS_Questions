// let arr = new Array(3);

// arr.push(10);

// console.log(arr);  // [3 * empty, 10] // three empty index and on 4th index 10 is added


let arr = new Array(5);

for(let i =0;i<5;i++) {
    arr[i] = Number(prompt("Enter value"));  // add element from the console
}
